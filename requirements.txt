# ─────────────────────────────────────────────────────────
# AI Illustration Agent — Python Dependencies
# Pin versions for reproducible builds in production.
# ─────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation (used by FastAPI)
pydantic==2.8.2

# Google Gemini SDK
google-generativeai==0.7.2

# HTTP client (used internally by google-generativeai)
httpx==0.27.2

# Async support
anyio==4.4.0

# Optional: structured logging (JSON format for Cloud Logging)
python-json-logger==2.0.7
